<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuddyBank - Your Personal Contact Manager</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #6366f1;
            --primary-dark: #4f46e5;
            --secondary-color: #f3f4f6;
            --text-color: #1f2937;
            --text-light: #6b7280;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            overflow: hidden;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-color);
            display: flex;
            align-items: stretch;
            justify-content: stretch;
            position: relative;
            overflow-x: hidden;
        }

        /* Background Pattern */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                radial-gradient(circle at 20px 20px, rgba(99, 102, 241, 0.1) 2px, transparent 0),
                radial-gradient(circle at 40px 40px, rgba(99, 102, 241, 0.1) 2px, transparent 0);
            background-size: 40px 40px;
            z-index: -1;
        }

        .app-container {
            background: var(--white);
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow: hidden;
        }

        /* Decorative Elements */
        .app-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 300px;
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(79, 70, 229, 0.1) 100%);
            clip-path: polygon(0 0, 100% 0, 100% 100%, 0 80%);
            z-index: 0;
        }

        .main-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 2rem;
            position: relative;
            z-index: 1;
            flex: 1;
            min-height: 60vh;
        }

        .logo-container {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            margin-bottom: 3rem;
        }

        .logo-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
        }

        .logo-text {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--text-color);
        }

        .hero {
            text-align: center;
            margin-bottom: 2rem;
            max-width: 800px;
            padding: 0 1rem;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }

        .hero p {
            color: var(--text-light);
            font-size: 1.25rem;
            margin-bottom: 3rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
        }

        .auth-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            margin-top: 2rem;
        }

        .btn {
            padding: 0.875rem 2rem;
            border-radius: 12px;
            font-size: 1.125rem;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            min-width: 160px;
        }

        .btn i {
            font-size: 1.25rem;
        }

        .btn-primary {
            background: var(--primary-color);
            color: white;
            border: none;
            box-shadow: 0 4px 6px -1px rgba(99, 102, 241, 0.2);
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 8px 12px -1px rgba(99, 102, 241, 0.3);
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
        }

        .btn-outline:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-2px);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            padding: 2rem;
            position: relative;
            z-index: 1;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
        }

        .feature-card {
            background: var(--white);
            padding: 2rem;
            border-radius: 16px;
            text-align: left;
            border: 1px solid #e5e7eb;
            box-shadow: 0 8px 16px -4px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            background: linear-gradient(to bottom right, #ffffff, #f9fafb);
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px -4px rgba(99, 102, 241, 0.2);
            border-color: var(--primary-color);
        }

        .feature-card i {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--text-color);
            font-weight: 600;
        }

        .feature-card p {
            color: var(--text-light);
            font-size: 1.1rem;
            line-height: 1.6;
        }

        /* Floating Shapes */
        .shape {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            opacity: 0.1;
            z-index: 0;
        }

        .shape-1 {
            width: 400px;
            height: 400px;
            top: -200px;
            right: -200px;
        }

        .shape-2 {
            width: 300px;
            height: 300px;
            bottom: -150px;
            left: -150px;
        }

        @media (max-width: 768px) {
            body {
                align-items: flex-start;
                overflow-y: auto;
            }

            .app-container {
                min-height: 100vh;
                height: auto;
            }

            .main-content {
                padding: 1.5rem;
                min-height: auto;
            }

            .logo-container {
                margin-bottom: 2rem;
            }

            .logo-icon, .logo-text {
                font-size: 2rem;
            }

            .hero h1 {
                font-size: 2.5rem;
                margin-bottom: 1rem;
            }

            .hero p {
                font-size: 1.1rem;
                margin-bottom: 2rem;
            }

            .auth-buttons {
                flex-direction: column;
                gap: 1rem;
                width: 100%;
                max-width: 300px;
            }

            .features {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                padding: 1.5rem;
                margin-top: 1rem;
            }

            .feature-card {
                padding: 1.5rem;
            }

            .feature-card h3 {
                font-size: 1.25rem;
            }

            .feature-card p {
                font-size: 1rem;
            }

            .shape-1, .shape-2 {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        
        <div class="main-content">
            <div class="logo-container">
                <i class="fas fa-address-book logo-icon"></i>
                <span class="logo-text">BuddyBank</span>
            </div>

            <div class="hero">
                <h1>Your Personal Contact Manager</h1>
                <p>Organize and manage your contacts with ease. A modern solution for your contact management needs.</p>

                <% if (!user) { %>
                    <div class="auth-buttons">
                        <a href="/signin" class="btn btn-primary">
                            <i class="fas fa-sign-in-alt"></i>
                            Sign In
                        </a>
                        <a href="/signup" class="btn btn-outline">
                            <i class="fas fa-user-plus"></i>
                            Sign Up
                        </a>
                    </div>
                <% } else { %>
                    <div class="auth-buttons">
                        <a href="/contacts" class="btn btn-primary">
                            <i class="fas fa-users"></i>
                            View Contacts
                        </a>
                        <a href="/sign-out" class="btn btn-outline">
                            <i class="fas fa-sign-out-alt"></i>
                            Sign Out
                        </a>
                    </div>
                <% } %>
            </div>
        </div>

        <div class="features">
            <div class="feature-card">
                <i class="fas fa-shield-alt"></i>
                <h3>Secure Authentication</h3>
                <p>Protected with industry-standard security measures and encrypted storage.</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-address-card"></i>
                <h3>Contact Management</h3>
                <p>Easily organize and manage your contacts with our intuitive interface.</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-cloud-upload-alt"></i>
                <h3>Cloud Storage</h3>
                <p>Access your contacts from anywhere, anytime. Your data is always synchronized.</p>
            </div>
        </div>
    </div>
</body>
</html> 